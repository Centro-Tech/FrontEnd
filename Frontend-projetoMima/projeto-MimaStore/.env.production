# Configurações de Produção - AWS
# Usa proxy local do Nginx que redireciona para o backend privado
# O Nginx é configurado via CI/CD com a variável IP_PORTA_API

# URL da API via proxy local (Nginx faz proxy para backend privado)
# Não precisa alterar isso - funciona dinamicamente via Nginx
VITE_API_URL=/api

# URL do bucket S3 onde as imagens são armazenadas
VITE_S3_BUCKET_URL=https://mimaastore-bucket-raw-2025.s3.us-east-1.amazonaws.com
