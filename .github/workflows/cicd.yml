name: CI/CD para EC2 pública e privada

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        host: ["host_a", "host_b"]  # nomes literais para a matrix

    steps:
      # 1️⃣ Checkout do código
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      # 2️⃣ Configuração do Node.js (para o frontend)
      - name: FE - Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3️⃣ Debug inicial
      - name: Verificar diretório atual
        run: |
          pwd
          ls -R

      # 4️⃣ Build da aplicação (React/Vite)
      - name: FE - Instalar Dependências e Fazer Build
        run: |
          npm install
          npm run build --if-present
        working-directory: Frontend-projetoMima/projeto-MimaStore

      # 5️⃣ Esperar a instância subir
      - name: Aguardar inicialização da EC2
        run: sleep 40

      # 6️⃣ Mapear host e chave SSH específica para cada instância
      - name: Definir host e chave SSH
        id: set_host
        run: |
          if [ "${{ matrix.host }}" = "host_a" ]; then
            echo "HOST_REAL=${{ secrets.REMOTE_HOST }}" >> $GITHUB_ENV
            echo "SSH_KEY=${{ secrets.EC2_SSH_KEY_A }}" >> $GITHUB_ENV
          else
            echo "HOST_REAL=${{ secrets.REMOTE_HOST_B }}" >> $GITHUB_ENV
            echo "SSH_KEY=${{ secrets.EC2_SSH_KEY_B }}" >> $GITHUB_ENV
          fi

      # 7️⃣ Copiar arquivos do frontend via SSH (rsync)
      - name: FE - Copiar arquivos e Deploy
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ env.SSH_KEY }}
          REMOTE_HOST: ${{ env.HOST_REAL }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          TARGET: /home/${{ secrets.REMOTE_USER }}/app
          SOURCE: "Frontend-projetoMima/projeto-MimaStore/"
          ARGS: "-rltgoDzvO --delete"

      # 8️⃣ Configurar Docker e rodar container
      - name: FE - Configurar Docker e rodar container
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ env.HOST_REAL }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ env.SSH_KEY }}
          script: |
            echo "🚀 Configurando Docker e subindo container..."

            # Instala Docker se não existir
            if ! command -v docker &> /dev/null; then
              echo "⚙️ Instalando Docker..."
              sudo yum install -y docker || sudo dnf install -y docker
              sudo systemctl enable docker
              sudo systemctl start docker
              sudo usermod -aG docker $USER
            fi

            cd /home/${{ secrets.REMOTE_USER }}/app

            # Para containers antigos
            docker stop mimastore || true
            docker rm mimastore || true

            # Build e run do novo container
            docker build -t mimastore-frontend .
            docker run -d -p 80:80 --name mimastore mimastore-frontend

            echo "✅ Deploy finalizado e container em execução!"
